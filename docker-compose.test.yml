---
services:
  mailpit:
    image: axllent/mailpit:v1.27@sha256:68c257dc344553a364a71c758acd18708f21073a78d0597e0120c3c92f03561f

  fetchmail:
    links:
      - mda:mda.local

  test:
    build: ./test
    volumes:
      - data-mail:/var/vmail:ro
      - data-tls:/media/tls:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      # For development
      - ./test/rootfs/usr/share/tests:/usr/share/tests:ro
    env_file: .env
    depends_on:
      - mailpit
      - fetchmail

  # For development
  # web:
  #   volumes:
  #     - ./web/rootfs/etc/nginx/nginx.conf.templ:/etc/nginx/nginx.conf.templ:ro
  #   ports:
  #     - "0.0.0.0:81:80"
