version: '2'

services:
  test:
    image: jeboehm/mailserver-test
    build: ./test
    links:
      - db
      - mta:example.com
      - mda
      - web
    volumes:
      - data-mail:/var/vmail:ro
      - data-tls:/media/tls:ro
    env_file: .env
