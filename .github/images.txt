# Third-party container images used by the integration tests.
#
# This list lives in its own file so that the Actions cache key is a function of
# the image list alone, instead of the workflow that happens to contain it.
#
# Keep in sync with deploy/compose/*.yaml, test/k8s/{mysql,pgsql}/ and
# test/k8s/base/mailpit.yaml.
mysql:lts
postgres:18-alpine
axllent/mailpit:v1.27
redis:8.2-alpine
ghcr.io/jeboehm/fetchmailmgr:0.4.0
