name: Lint YAMLs

on:
  pull_request:
    paths:
      - '**.yml'
      - '**.yaml'

jobs:
  yamllint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        
      - name: Run linter
        run: |
          make lint-yaml
